/* gen-code-types.tpc */

/* Test file for TPC */
    int somme,produit,quotient,reste,difference ;
    char letter, digit, punct;
	int p_compteur;
	int indice, p_indice;
    
int main(void)  {
	if(letter < somme);
	if(1 < 2);
	else;
}
